"use strict";
window.addEventListener("load", () => {
	// Variables
	const rutaSessionCookies="./api	"
	
	// Eventos

	// Novedad en algún lado

		// 1. Si la novedad está en el filtro personalizado
			// Si el filtro personalizado es el Estándar, inactiva los íconos 'modificar nombre' y 'eliminar'
			// De lo contrario, 'des-inactiva' los iconos 'modificar nombre' y 'eliminar'
			// Inactiva las opciones 'reiniciar' y 'actualizar'

			// Obtiene las opciones de la BD y actualiza los filtros

		// 2. Si la novedad está en cualquier otro campo...
			// Si el filtro personalizado es el Estándar
			// Desaparece el nombre de la vista e inactiva todos los íconos menos 'nuevo'

			// De lo contrario, 
				// Si el filtro personalizado es uno definido,
					// Inactiva la opcion de 'Modificar nombre' en la vista
					// Activa la opcion de recargar y guardar
				// De lo contrario, no hace nada
				
		
		// 3. Comunes
			// Obtiene los valores de todos los filtros elegidos

			// Actualiza la session y cookie
			
			// Obtiene la información a mostrar

			// Actualiza el contador

			// Actualiza la información mostrada

			// Actualiza el ID del Filtro en el registro de usuario

	});
