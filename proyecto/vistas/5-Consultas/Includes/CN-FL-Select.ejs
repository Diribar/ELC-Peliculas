<div id="<%= campo.codigo %>" class="pref contSelect ocultaFiltro <%- locals.color ? color : '' %>">
	<select name="<%= campo.codigo %>" class="<%= locals.eventual ? campo.codigo : 'presenciaEstable' %> prefSimple">
		<% if (campo.titulo) { %>
			<option value=""><%= campo.titulo %></option>
			<option value="" class="sinFiltro">Quitar la opción elegida</option>
		<% } %>
		<% for (let opcion of campo.opciones) { %>
			<option value="<%= opcion.id %>"><%= opcion.nombre %></option>
		<% } %>
	</select>
</div>
