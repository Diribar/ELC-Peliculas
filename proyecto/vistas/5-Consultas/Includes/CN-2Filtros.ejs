<!--Filtros Guardados-->
<div id="configCabecera">
	<div id="configNueva" class="relative">
		<input name="nombreNuevo" type="text" class="absolute">
	</div>
	<select name="configCons_id">
		<option value="">Filtros Guardados...</option>
		<!-- Configuraciones personales -->
		<optgroup id="propios" label="Configuraciones Propias" class="<%- !configsDeCabecera.propios.length ? 'ocultar' : '' %>">
			<% for (let configDeCabecera of configsDeCabecera.propios) { %>
				<option
					value="<%= configDeCabecera.id %>"
					<% if (configDeCabecera.id == configCons_id) { %>selected<% } %>
				><%= configDeCabecera.nombre %></option>
			<% } %>
		</optgroup>
		<!-- Configuraciones predeterminadas -->
		<% if (userID != 1) { %>
			<optgroup label="Provistas por Nuestro Sitio">
				<% for (let configDeCabecera of configsDeCabecera.predeterms) { %>
					<option
						value="<%= configDeCabecera.id %>"
						<% if (configDeCabecera.id == configCons_id) { %>selected<% } %>
					><%= configDeCabecera.nombre %></option>
				<% } %>
			</optgroup>
		<% } %>
	</select>
	<div id="iconosBotonera" class="flexRow">
		<i id="nuevo" class="fa-solid fa-file scale inactivo" title="Crea un filtro nuevo"></i>
		<i id="deshacer" class="fa-solid fa-rotate-left scale inactivo" title="Deshace los cambios"></i>
		<i id="guardar" class="fa-solid fa-floppy-disk scale inactivo" title="Guarda los cambios"></i>
		<i id="edicion" class="fa-solid fa-pen scale inactivo" title="Edición del nombre"></i>
		<i id="eliminar" class="fa-solid fa-trash-can scale inactivo" title="Elimina el filtro actual"></i>
	</div>
</div>

<!-- Campos -->
<div id="configCampos" class="flexCol sectorConDesplV">
	<nav class="startUp flexCol ocultar">
		<!-- PRIORITARIOS -->
		<!-- Calidad de la Imagen -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.calidadImagen}) %>
		<% if (locals.usuario) { %>
			<!-- Preferencia por Película -->
			<%- include('CN-FL-Selects', {campos: configsConsCampos.pppOpciones, eventual: true}) %>
		<% } %>
		<!-- Idioma -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.idioma, eventual: true}) %>

		<!-- RELACIONADO CON LA IGLESIA CATÓLICA -->
		<!-- Relación con la Iglesia Católica -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.cfc, eventual: true, color: 'cfc'}) %>
		<!-- Proceso de Canonización -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.canons, eventual: true, color: 'cfc'}) %>
		<!-- Roles Iglesia -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.rolesIgl, eventual: true, color: 'cfc'}) %>
		<!-- Aparición Mariana -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.apMar, eventual: true, color: 'cfc'}) %>

		<!-- HECHOS REALES -->
		<!-- Basado en Hechos Reales -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.bhr, eventual: true, color: 'bhr'}) %>
		<!-- Epoca de Ocurrencia -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.epocasOcurrencia, color: 'bhr'}) %>

		<!-- ATRIBUTOS DE LA PELÍCULA -->
		<!-- Público Recomendado -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.publicos}) %>
		<!-- Época de Estreno -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.epocasEstreno}) %>
		<!-- Tipo de Actuación -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.tiposActuacion}) %>
		<!-- Películas o Colecciones -->
		<%- include('CN-FL-Selects', {campos: configsConsCampos.entidad}) %>

		<!-- Palabras Clave -->
		<div id="palabrasClave" class="pref ocultaFiltro">
			<input type="text" name="palabrasClave" class="prefSimple" placeholder="Buscar por palabras clave">
			<i class="fa-solid fa-circle-right inactivo scale" title="Aplicar las palabras"></i>
		</div>
	</nav>
</div>

<!-- Muestra / Oculta filtros -->
<div id="toggleFiltrosIndivs" class="flexCol ocultar">
	<div id="mostrarFiltros" class="pointer scale">
		<em>Click</em> para <span>ver</span><br>más filtros
	</div>
	<div id="ocultarFiltros" class="pointer scale ocultaFiltros">
		<em>Click</em> para <span>ocultar</span><br>los filtros sin usar
	</div>
</div>
