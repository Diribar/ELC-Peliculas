<!-- Comentario -->
<section id="comentario" class="flexCol">
    <div id="comenTitulo">
        <h3>Comentario</h3>
        <p><span id="contador"><%= largoComentario %></span>/<%= largoComentario %></p>
    </div>
    <textarea
        name="comentario"
        <% if (["inactivar", "eliminar", "rechazar", "cambiarMotivo"].includes(codigo)) { %>readonly<% } %>
        placeholder="Agregá un comentario"
    ><%- locals.comentario ? comentario : "" %></textarea>
</section>