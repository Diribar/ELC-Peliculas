<!-- Comentario -->
<section id="comentario" class="flexCol <% if (["inactivar", "eliminar", "rechazar"].includes(codigo)) { %>invisible<% } %>">
    <div class="flexRow spaceBetween">
        <h3>Comentario</h3>
        <p><span id="pendiente">100</span>/100</p>
    </div>
    <textarea name="comentario" <% if (["inactivar", "rechazar"].includes(codigo)) { %>readonly<% } %>></textarea>
</section>