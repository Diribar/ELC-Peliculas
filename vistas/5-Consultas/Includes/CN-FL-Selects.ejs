<div id="<%= asunto.codigo %>" class="pref contSelect <%- locals.color ? color : '' %> ocultaFiltros">
	<select name="<%= asunto.codigo %>" class="<%= locals.eventual ? asunto.codigo : 'presenciaEstable' %> prefSimple">
		<option value="" class="ocultar"><%= asunto.titulo %></option>
		<option value="" id="sinFiltro">Quitar la opción elegida</option>
		<% for (let opcion of asunto.opciones) { %>
				<option value="<%= opcion.id %>"><%= opcion.nombre %></option>
		<% } %>
	</select>
</div>
