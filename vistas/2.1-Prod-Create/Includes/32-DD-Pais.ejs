<section id="paises_id">
	<label>País/es de origen</label>
	<div class="input-error">
		<% if (dataEntry.paises_id) { %>
			<!-- Lugar donde mostrar los nombres -->
			<input type="text" value="<%= paises %>" disabled class="input">
			<%- include("39-DD-Mensajes") %>
		<% } else { %>
			<!-- Lugar donde almacenar los ID -->
			<input name="paises_id" type="text" class="ocultar input" >
			<!-- Lugar donde mostrar los nombres -->
			<input id="mostrarPaises" type="text" readonly tabindex="-1">
			<!-- Opciones de países -->
			<select autofocus>
				<option value="" selected class="ocultar">Elejí un país</option>
				<option value="borrar">Borrar todo/s</option>
				<% continentes = ["América", "Europa", "Africa", "Asia", "Oceanía"] %> 
				<% for (let continente of continentes) { %>
					<optgroup label="<%= continente %>">
						<% for (let pais of paises.filter(n => n.continente == continente)) { %>
							<option value="<%= pais.id %>"><%= pais.nombre %></option>
						<% } %>	
					</optgroup>
				<% } %> 
			</select>
		<% } %>
		<i class="fa-solid fa-circle-xmark<%- !errores.paises_id ? ' ocultar' : '' %>">
			<span class="mensajeError"><%= errores.paises_id %></span>
		</i>
	</div>
</section>
