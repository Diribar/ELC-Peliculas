<% if (regs && regs.length) { %>
	<h3><%= titulo %> (<%= regs.length %>)</h3>
	<% for (let reg of regs) { %>
		<a href="/<%= url %>/?entidad=<%= reg.entidad %>&id=<%= reg.id + (reg.edicion_id ? ('&edicion_id=' + reg.edicion_id) : '') + (locals.origen ? ('&origen='+origen) : '') %>">
			<p><%= reg.fechaRefTexto %> - <%= reg.abrev %> - <%= reg.nombre %></p>
		</a>
	<% } %>	
<% } %>