<div id="tituloInfoDetalle" class="relative">
	<% const oa = locals.entidad == "capitulos" ? 'o' : 'a' %>
	<h3>Links para verl<%= oa %></h3>
</div>
<div id="links" class="marco flexCol">
	<!-- Película/Capítulo -->
	<% if (entidad != "colecciones") { %>
		<div class="flexRowAlign">
			<% if (links.TR.length) { %><h4>Película</h4><% } %>
			<% if (links.PL.length) { %>
				<!-- Ayuda -->
				<div id="ayudaLinks">
					<i class="fa-solid fa-circle-question ayudaClick desplClick">
						<div class="ayudaMostrar ocultar desplMostrar">
							<p><em>Color del borde de los logos (simil semáforo):</em></p>
							<ul type="dots">
								<li><i class="fa-solid fa-circle enCast"></i> en idioma <b>castellano</b></li>
								<li><i class="fa-solid fa-circle subtCast"></i> <b>subtitulos</b> en castellano</li>
								<li><i class="fa-solid fa-circle otroIdioma"></i> en <b>otro</b> idioma</li>
							</ul>
							<p>Si colocás el mouse sobre la imagen de algún link, te damos información específica de él</p>
						</div>
					</i>
				</div>
				<!-- Íconos -->
				<%- include("PR1-DetalleLinks", {linksProd: links.PL}) %>
			<% } else { %>
				<p><em>- No disponemos de links. ¿Podés aportarnos alguno? -</em></p>
			<% } %>
		</div>
	<% } %>
	<% if (links.TR.length) { %>
		<!-- Trailer -->
		<div class="flexRowAlign">
			<h4>Trailer</h4>
			<%- include("PR1-DetalleLinks", {linksProd: links.TR}) %>
		</div>
	<% } %>
	<% if (entidad == "colecciones" && !links.TR.length) { %>
		<p><em>- No disponemos de links de Trailer de esta colección. -</em></p>
	<% } %>
</div>
<div id="marcoDetalle">
	<div id="etiquetas" class="flexRow">
		<p class="resaltar">Sinopsis</p>
		<p>Información General</p>
		<p>Actores</p>
		<!-- RCLVs -->
		<% for (let i=0; i < asocs.length; i++) { %>
			<% if (bloqueIzq[asocs[i]]) { %>
				<p><%= rclvsNombre[i] %></p>
			<% } %>
		<% } %>
	</div>
	<div id="datosDetalle">
		<div id="sinopsis" class="datosParcial">
			<p><%= registro.sinopsis ? registro.sinopsis : 'No disponemos de esa información' %></p>
		</div>
		<div id="masInfo" class="datosParcial" style="display: none;">
			<% if (!bloqueIzq.masInfoIzq.length) { %>
				<p>No disponemos de esa información</p>
			<% } else { %>
				<div id="masInfoIzq">
					<% for (let campo of bloqueIzq.masInfoIzq) { %>
						<p><%= campo.titulo %>: <b><%= campo.valor %></b></p>
					<% } %>
				</div>
			<% } %>
			<% if (bloqueIzq.masInfoDer.length) { %>
				<div id="masInfoDer">
					<% for (let campo of bloqueIzq.masInfoDer) { %>
						<p><%= campo.titulo %>: <b><%= campo.valor %></b></p>
					<% } %>
				</div>
			<% } %>
		</div>
		<div id="actores" class="datosParcial" style="display: none;">
			<p><%= bloqueIzq.actores ? bloqueIzq.actores : 'No disponemos de esa información' %></p>
		</div>
		<% for (RCLV of RCLVs) { %>
			<% if (bloqueIzq[RCLV.asociacion]) { %>
				<div id="rclv" class="datosParcial" style="display: none;">
					<% for (let campo of bloqueIzq[RCLV.asociacion]) { %>
						<p>
							<%= campo.titulo %>: <b><%= campo.valor %></b>
							<% if (campo.titulo == bloqueIzq[RCLV.asociacion][0].titulo) { %>
								<span>
									<a href="/rclv/detalle/?entidad=<%= RCLV.entidad %>&id=<%= + registro[RCLV.campo_id] %>">
										<i class="fa-solid fa-circle-info" tabindex="-1" title="Ir a la vista 'Detalle'"></i>
									</a>
								</span>
							<% } %>
						</p>
					<% } %>
				</div>
			<% } %>
		<% } %>
	</div>
</div>