<% registro = camposDA.find(n => n.nombre == campo) %>
<% let {titulo, valores, grupo} = registro %>
<section id="<%= campo %>" class="label-input">
	<label><%= titulo %></label>
	<div class="inputError">
		<!-- Input -->
		<select name="<%= campo %>" class="input">
			<% for (let grupo of grupos) { %>
				<optgroup label="<%= grupo.label %>" class="<%= grupo.clase %>">
					<% for (let valor of grupo.valores) { %>
						<option value="<%= valor.id %>" class="RCLV <%= valor.clase %>" <% if (dataEntry[campo] == valor.id) { %>selected<% } %>><%= valor.nombre %></option>
					<% } %>
				</optgroup>
			<% } %>
		</select>

		<%- include("PA4-DA9-Ayuda") %>
		<!-- Link a Relación con la vida -->
		<i id="alta" class="fa-solid fa-circle-plus linkRCLV <%= campo %>" title='Agregar "<%= titulo.toLowerCase() %>"' tabindex="-1"></i>
		<i id="edicion" class="fa-solid fa-square-pen linkRCLV ocultar <%= campo %>" title='Editar "<%= titulo.toLowerCase() %>"' tabindex="-1"></i>

		<!-- Aviso de cambio en el data-entry -->
		<i class="fa-solid fa-arrow-right-long ocultar"></i>
	</div>
</section>
