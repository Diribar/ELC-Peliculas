<section class="label-input">
	<label>Idioma original</label>
	<div class="input-error">
		<select name="idioma_original_id" class="input">
			<optgroup label="Más frecuentes">
				<% for (idioma of BD_idiomas.filter(n => n.mas_frecuente)) { %>
					<option value="<%= idioma.id %>" <% if (idioma_id==idioma.id) { %>selected<% } %>><%= idioma.nombre %></option>
				<% } %>	
			</optgroup>
			<optgroup label="Demás idiomas">
				<% for (idioma of BD_idiomas.filter(n => !n.mas_frecuente)) { %>
					<option value="<%= idioma.id %>" <% if (idioma_id==idioma.id) { %>selected<% } %>><%= idioma.nombre %></option>
				<% } %>	
			</optgroup>
		</select>
		<!-- Ayuda -->
		<%- (registro.idioma_original_id && !errores.idioma_original_id) ? include("39-DD-Mensajes") : "" %>
		<!-- Errores -->
		<i class="fas fa-times-circle<%- !errores.idioma_original_id ? ' ocultar' : '' %>">
			<span class="mensajeError"><%= errores.idioma_original_id %></span>
		</i>
		<i class="fas fa-check-circle ocultar"></i>
	</div>
</section>
