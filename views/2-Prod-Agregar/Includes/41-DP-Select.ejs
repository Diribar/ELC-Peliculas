<% registro = datosPersSelect.find(n => n.campo == campo) %>
<section class="label-input flex-row<% if (registro.RCLV) { %> ocultar RCLV<% } %>">
	<label><%= registro.titulo %></label>
	<div class="input-error">
		<!-- Input -->
		<select name="<%= campo %>" class="input <%= registro.clase ? registro.clase : '' %>" <% if (dataEntry[registro.campo] && !errores[registro.campo]) { %>disabled<% } else { %>autofocus<% } %>>
			<option value="" class="ocultar" <% if (!dataEntry[campo]) { %>selected<% } %>>Elegí una opción</option>
			<% if (campo == "valor_id") { %><option value="1" <% if (dataEntry[campo]==1) { %>selected<% } %>>Ninguno del listado</option><% } %> 
			<% for (valor of registro.valores) { %>
				<option value="<%= valor.id %>" 
					<% if (dataEntry[campo] == valor.id) { %>selected<% } %>
					<% if (campo == "subcategoria_id") { %>class="<%= valor.categoria_id %>"<% } %>
					<% if (dataEntry.entidad == "peliculas" && (campo == "en_castellano_id" || campo == "en_color_id") && valor.id == 2) { %>class="ocultar"<% } %>
				>
					<%= valor.nombre %>
				</option>
			<% } %>
		</select>
		<!-- Ayuda -->
		<i class="fas fa-question-circle"></i>
		<ul type="dots" class="mensajeAyuda ocultar">
			<% if (registro.mensajePeli) {registro.mensajes = (dataEntry.entidad == "peliculas") ? registro.mensajePeli : registro.mensajeColec} %>
			<% for (mensaje of registro.mensajes) { %><li><%= mensaje %></li><% } %> 
		</ul>
		<!-- Errores -->
		<i class="fas fa-times-circle<%- !errores[campo] ? ' ocultar' : '' %>">
			<span class="mensajeError"><%= errores[campo] %></span>
		</i>
		<i class="fas fa-check-circle ocultar" style="display: none;"></i>
		<!-- Link a Relación con la vida -->
		<% if (registro.RCLV) { %>
			<a class="link <%= campo %>" title='Link a "Agregar <%= registro.titulo.toLowerCase() %>"' tabindex="-1"><i class="fas fa-plus-circle"></i></a>
		<% } %>
	</div>
</section>
