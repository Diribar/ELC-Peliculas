window.addEventListener("load", () => {
	// Definir las variables
	let form = document.querySelector("form");
	let button = document.querySelector("form button[type='submit']");
	let iconoError = document.querySelectorAll(".input-error .fa-times-circle");
	let mensajesError = document.querySelectorAll(".input-error .mensajeError");

	// Otras variables
	let ruta = "/producto/agregar/api/validar-datos-duros/?";

	// Respuestas ante cambios 'change' en el form

		// 1. Que muestre el OK/Error

		// 2. Consecuencias en los íconos de 'flechas'

	// Respuestas de los íconos de 'flechas'
})